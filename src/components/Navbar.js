import { Link, useNavigate } from "react-router-dom";

function Navbar() {

  const Navbar = () => {
  return (
    <div style={{
      background: "white",
      padding: 15,
      boxShadow: "0 2px 6px rgba(0,0,0,0.1)"
    }}>
      <h3>Secure Online Voting Platform</h3>
    </div>
  );
};

};

export default Navbar;
